/** The gentle weekly-review item under the sidebar sections. */
export function RitualSidebarItem(_: { rail: boolean }) {
  return null;
}

/** Ritual settings (weekly review day, journal in search). */
export function RitualSettings() {
  return null;
}
